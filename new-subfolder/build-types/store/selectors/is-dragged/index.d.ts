import { StoreState } from '../../types';
/**
 * Check if module with given id is currently dragged.
 *
 * @since ??
 *
 * @param {StoreState} state State.
 * @param {string} id Module id.
 *
 * @returns {boolean}
 */
export declare const isDragged: (state: StoreState, id: string) => boolean;

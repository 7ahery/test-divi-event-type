import { StoreState } from '../../types';
/**
 * Get dragging status - is there module that is being dragged right now.
 *
 * @since ??
 *
 * @param {StoreState} state Store state.
 *
 * @returns {boolean}
 */
export declare const getDraggingStatus: (state: StoreState) => boolean;

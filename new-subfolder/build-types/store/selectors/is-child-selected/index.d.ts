import { StoreState } from '../../types';
/**
 * Check if module with given id is currently have its child module selected.
 *
 * @since ??
 *
 * @param {StoreState} state State.
 * @param {string} id ID.
 *
 * @returns {boolean}
 */
export declare const isChildSelected: (state: StoreState, id: string) => boolean;

import { HoveredModule, StoreState } from '../../types';
/**
 * Get currently hovered module.
 *
 * @since ??
 *
 * @param {StoreState} state State.
 *
 * @returns {HoveredModule}
 */
export declare const getHoveredModule: (state: StoreState) => HoveredModule;

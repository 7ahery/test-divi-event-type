import { ActionObject } from './actions/types';
import { StoreState } from './types';
/**
 * Reducer.
 *
 * @since ??
 * @param {StoreState} state Previous State.
 * @param {ActionObject} action Action.
 *
 * @returns {StoreState}
 */
export declare const reducer: (state: StoreState, action?: ActionObject) => StoreState;

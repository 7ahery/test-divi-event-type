import { DraggedModules } from '../../types';
import { StartDrag } from './types';
/**
 * Set start drag (and drop) state on store.
 *
 * @since ??
 *
 * @param {DraggedModules} items Dragged items.
 *
 * @returns {StartDrag}
 */
export declare const startDrag: (items: DraggedModules) => StartDrag;

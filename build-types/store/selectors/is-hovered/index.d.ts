import { StoreState } from '../../types';
/**
 * Check if module with given id is currently hovered.
 *
 * @since ??
 *
 * @param {StoreState} state State.
 * @param {string} id ID.
 *
 * @returns {boolean}
 */
export declare const isHovered: (state: StoreState, id: string) => boolean;

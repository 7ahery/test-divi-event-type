import { SetCursorPosition } from './types';
/**
 * Set cursor position.
 *
 * @since ??
 *
 * @param {number} pageX Mouse event's pageX.
 * @param {number} pageY Mouse event's pageY.
 * @param {number} clientX Mouse event's clientX.
 * @param {number} clientY Mouse event's clientY.
 *
 * @returns {SetCursorPosition}
 */
export declare const setCursorPosition: (pageX: number, pageY: number, clientX: number, clientY: number) => SetCursorPosition;

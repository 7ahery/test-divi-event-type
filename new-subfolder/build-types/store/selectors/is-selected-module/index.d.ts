import { StoreState } from '../../types';
/**
 * Check if given module is selected.
 *
 * @since ??
 *
 * @param {StoreState} state Store state.
 * @param {string} id Module id.
 *
 * @returns {boolean}
 */
export declare const isSelectedModule: (state: StoreState, id: string) => boolean;

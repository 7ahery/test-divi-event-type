import { StoreState } from '../../types';
/**
 * Get currently dragged modules.
 *
 * @since ??
 *
 * @param {StoreState} state Store state.
 *
 * @returns {DraggedModules}
 */
export declare const getDraggedModules: (state: StoreState) => StoreState['draggedModules'];

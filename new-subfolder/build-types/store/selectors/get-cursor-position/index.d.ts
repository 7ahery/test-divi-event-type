import { CursorPosition, StoreState } from '../../types';
/**
 * Get cursor position.
 *
 * @since ??
 *
 * @param {StoreState} state Store state.
 *
 * @returns {CursorPosition}
 */
export declare const getCursorPosition: (state: StoreState) => CursorPosition;
